import { EventsFilterPipePipe } from './events-filter-pipe.pipe';

describe('EventsFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EventsFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
